•	Maintain a short CHANGELOG for each delivery: file list + rationale.
•	If a file becomes smaller: confirm no functionality dropped (compare blocks & endpoints).
•	Never delete UI blocks without explicit approval.
•	Update /docs artifacts (README, DATA_MODEL, TEST_PLAN, etc.) whenever new queues/exports/snapshots ship so public repo stays in sync.
