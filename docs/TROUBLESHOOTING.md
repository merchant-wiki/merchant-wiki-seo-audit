•	Fatal: Serialization of ‘Closure’ — uninstall hook must be static (no closure).
•	Sitemap shows extra blank lines — ensure no echo/whitespace before headers; verify extract tool strips BOM/leading newlines.
•	Counts = 0 — check DB prefix in Debug; Self Test; confirm SELECT/WRITE in Structure Check.
•	Export expired link — increase admin_url nonces; ensure wp_nonce_url and check_admin_referer match.
