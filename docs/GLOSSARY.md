•	Inventory — set of site URLs collected into ${prefix}mw_url_inventory.
•	Status — per URL signals from sitemaps/robots/HTTP/meta.
•	PC Map — mapping post → primary category.
•	EWMA — exponentially weighted moving average of per item processing time.
•	Pill — colored label indicating health state.
